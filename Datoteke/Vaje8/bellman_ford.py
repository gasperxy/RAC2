def bellman_ford(graf, s):
    """
    Prejme graf in začetno vozlišče (s). Graf je predstavljen kot
    matrika sosednosti.
    """
    n = len(graf)
    razdalje_do = [float("inf")] * n
    povezava_do = [None] * n
    razdalje_do[s] = 0
    for i in range(n):
        continue
    return None